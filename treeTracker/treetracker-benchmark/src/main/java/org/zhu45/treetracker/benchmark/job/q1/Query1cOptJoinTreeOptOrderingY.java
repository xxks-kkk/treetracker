package org.zhu45.treetracker.benchmark.job.q1;

import org.apache.commons.lang3.tuple.Pair;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinNode;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinOrderedGraph;
import org.zhu45.treetracker.benchmark.JoinFragmentContext;
import org.zhu45.treetracker.benchmark.Query;
import org.zhu45.treetracker.benchmark.job.JOBQueries;
import org.zhu45.treetracker.relational.operator.Operator;
import org.zhu45.treetracker.relational.operator.TupleBasedLeftSemiHashJoinOperator;
import org.zhu45.treetracker.relational.planner.Plan;
import org.zhu45.treetracker.relational.planner.rule.SemiJoinOrdering;

import java.util.Arrays;
import java.util.List;

import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getCompanyTypeInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getInfoTypeInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getMovieCompaniesInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getMovieInfoIdxInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getTitleInt;
import static org.zhu45.treetracker.common.DirectedEdge.asDirectedEdge;

/**
 * Generated by GenerateJOBBenchmarkWithOptimalJoinTrees
 *
 * imdb.q1c_title(movie_id,kind_id)
 * |imdb.q1c_movie_companies(movie_id,company_id,company_type_id)
 * ||imdb.q1c_company_type(company_type_id)
 * |imdb_int.movie_info_idx(movie_id,info_type_id)
 * ||imdb.q1c_info_type(info_type_id)
 */
public class Query1cOptJoinTreeOptOrderingY
        extends Query
{
    public Query1cOptJoinTreeOptOrderingY(JoinFragmentContext context)
    {
        super(context);
    }

    @Override
    protected Pair<Plan, List<Operator>> constructQuery()
    {
        MultiwayJoinNode movieCompaniesNode = getMovieCompaniesInt(JOBQueries.Q1c, null);
        MultiwayJoinNode companyTypeNode = getCompanyTypeInt(JOBQueries.Q1c);
        MultiwayJoinNode titleNode = getTitleInt(JOBQueries.Q1c, null);
        MultiwayJoinNode movieInfoIdxNode = getMovieInfoIdxInt(JOBQueries.Q1c, null);
        MultiwayJoinNode infoTypeNode = getInfoTypeInt(JOBQueries.Q1c, null);

        List<MultiwayJoinNode> traversalList = List.of(titleNode, movieCompaniesNode, movieInfoIdxNode, companyTypeNode, infoTypeNode);

        MultiwayJoinOrderedGraph orderedGraph = new MultiwayJoinOrderedGraph(titleNode, Arrays.asList(
                asDirectedEdge(titleNode, movieCompaniesNode),
                asDirectedEdge(movieCompaniesNode, companyTypeNode),
                asDirectedEdge(titleNode, movieInfoIdxNode),
                asDirectedEdge(movieInfoIdxNode, infoTypeNode)), traversalList);
        SemiJoinOrdering semiJoinOrdering = new SemiJoinOrdering(List.of(Pair.of(movieInfoIdxNode, infoTypeNode), Pair.of(movieCompaniesNode, companyTypeNode), Pair.of(titleNode, movieCompaniesNode), Pair.of(titleNode, movieInfoIdxNode)), orderedGraph);

        Plan plan = createPhysicalPlanForYannakakis(semiJoinOrdering, TupleBasedLeftSemiHashJoinOperator.class, false, false);

        verifyJoinOrdering(plan, Arrays.asList(titleNode.getSchemaTableName(),
                movieCompaniesNode.getSchemaTableName(),
                movieInfoIdxNode.getSchemaTableName(),
                companyTypeNode.getSchemaTableName(),
                infoTypeNode.getSchemaTableName()));

        return Pair.of(plan, plan.getOperatorList());
    }
}
