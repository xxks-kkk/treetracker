package org.zhu45.treetracker.benchmark.tpch;

import org.apache.commons.lang3.tuple.Pair;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinNode;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinOrderedGraph;
import org.zhu45.treetracker.benchmark.JoinFragmentContext;
import org.zhu45.treetracker.benchmark.Query;
import org.zhu45.treetracker.benchmark.job.TableInstanceId;
import org.zhu45.treetracker.relational.operator.Operator;
import org.zhu45.treetracker.relational.planner.Plan;

import java.util.Arrays;
import java.util.List;

import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getCustomer;
import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getLineitem;
import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getNation;
import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getOrders;
import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getSupplier;
import static org.zhu45.treetracker.common.DirectedEdge.asDirectedEdge;
import static org.zhu45.treetracker.relational.planner.rule.JoinOrdering.getJoinOrderingFromNodes;

/**
 * Generated by GenerateBenchmarkWithOptimalJoinTrees
 *
 * tpch.q7aw_nation2(nationkey2)
 * |tpch.q7aw_customer(custkey,nationkey2)
 * ||tpch_int.orders(orderkey,custkey)
 * |||tpch.q7aw_lineitem(orderkey,partkey,suppkey)
 * ||||tpch_int.supplier(suppkey,nationkey)
 * |||||tpch.q7aw_nation1(nationkey)
 */
public class Query7aWOptJoinTreeOptOrderingShallowHJOrdering
        extends Query
{
    public Query7aWOptJoinTreeOptOrderingShallowHJOrdering(JoinFragmentContext context)
    {
        super(context);
    }

    @Override
    protected Pair<Plan, List<Operator>> constructQuery()
    {
        MultiwayJoinNode nationNode1 = getNation(TPCHQueries.Q7aW, TableInstanceId.ONE);
        MultiwayJoinNode nationNode2 = getNation(TPCHQueries.Q7aW, TableInstanceId.TWO);
        MultiwayJoinNode customerNode = getCustomer(TPCHQueries.Q7aW);
        MultiwayJoinNode lineItemNode = getLineitem(TPCHQueries.Q7aW);
        MultiwayJoinNode supplierNode = getSupplier(TPCHQueries.Q7aW);
        MultiwayJoinNode ordersNode = getOrders(TPCHQueries.Q7aW);

        List<MultiwayJoinNode> traversalList = List.of(nationNode2, customerNode, ordersNode, lineItemNode, supplierNode, nationNode1);

        MultiwayJoinOrderedGraph orderedGraph = new MultiwayJoinOrderedGraph(nationNode2, Arrays.asList(
                asDirectedEdge(nationNode2, customerNode),
                asDirectedEdge(customerNode, ordersNode),
                asDirectedEdge(ordersNode, lineItemNode),
                asDirectedEdge(lineItemNode, supplierNode),
                asDirectedEdge(supplierNode, nationNode1)), traversalList);

        Plan plan = createPhysicalPlanFromJoinOrdering(
                getJoinOrderingFromNodes(traversalList), orderedGraph);

        verifyJoinOrdering(plan, Arrays.asList(nationNode2.getSchemaTableName(),
                customerNode.getSchemaTableName(),
                ordersNode.getSchemaTableName(),
                lineItemNode.getSchemaTableName(),
                supplierNode.getSchemaTableName(),
                nationNode1.getSchemaTableName()));

        return Pair.of(plan, plan.getOperatorList());
    }
}
