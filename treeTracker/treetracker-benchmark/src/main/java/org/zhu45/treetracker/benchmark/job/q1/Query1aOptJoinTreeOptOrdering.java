package org.zhu45.treetracker.benchmark.job.q1;

import org.apache.commons.lang3.tuple.Pair;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinNode;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinOrderedGraph;
import org.zhu45.treetracker.benchmark.JoinFragmentContext;
import org.zhu45.treetracker.benchmark.Query;
import org.zhu45.treetracker.benchmark.job.JOBQueries;
import org.zhu45.treetracker.relational.operator.Operator;
import org.zhu45.treetracker.relational.planner.Plan;

import java.util.Arrays;
import java.util.List;

import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getCompanyTypeInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getInfoTypeInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getMovieCompaniesInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getMovieInfoIdxInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getTitleInt;
import static org.zhu45.treetracker.common.DirectedEdge.asDirectedEdge;
import static org.zhu45.treetracker.relational.planner.rule.JoinOrdering.getJoinOrderingFromNodes;

/**
 * Generated by GenerateJOBBenchmarkWithOptimalJoinTrees
 *
 * imdb_int.movie_info_idx(movie_id,info_type_id)
 * |imdb.q1a_info_type(info_type_id)
 * |imdb.q1a_movie_companies(movie_id,company_id,company_type_id)
 * ||imdb.q1a_company_type(company_type_id)
 * ||imdb_int.title(movie_id,kind_id)
 */
public class Query1aOptJoinTreeOptOrdering
        extends Query
{
    public Query1aOptJoinTreeOptOrdering(JoinFragmentContext context)
    {
        super(context);
    }

    @Override
    protected Pair<Plan, List<Operator>> constructQuery()
    {
        MultiwayJoinNode movieCompaniesNode = getMovieCompaniesInt(JOBQueries.Q1a, null);
        MultiwayJoinNode companyTypeNode = getCompanyTypeInt(JOBQueries.Q1a);
        MultiwayJoinNode titleNode = getTitleInt(JOBQueries.Q1a, null);
        MultiwayJoinNode movieInfoIdxNode = getMovieInfoIdxInt(JOBQueries.Q1a, null);
        MultiwayJoinNode infoTypeNode = getInfoTypeInt(JOBQueries.Q1a, null);

        List<MultiwayJoinNode> traversalList = List.of(movieInfoIdxNode, infoTypeNode, movieCompaniesNode, companyTypeNode, titleNode);

        MultiwayJoinOrderedGraph orderedGraph = new MultiwayJoinOrderedGraph(movieInfoIdxNode, Arrays.asList(
                asDirectedEdge(movieInfoIdxNode, infoTypeNode),
                asDirectedEdge(movieInfoIdxNode, movieCompaniesNode),
                asDirectedEdge(movieCompaniesNode, companyTypeNode),
                asDirectedEdge(movieCompaniesNode, titleNode)), traversalList);

        Plan plan = createPhysicalPlanFromJoinOrdering(
                getJoinOrderingFromNodes(traversalList), orderedGraph);

        verifyJoinOrdering(plan, Arrays.asList(movieInfoIdxNode.getSchemaTableName(),
                infoTypeNode.getSchemaTableName(),
                movieCompaniesNode.getSchemaTableName(),
                companyTypeNode.getSchemaTableName(),
                titleNode.getSchemaTableName()));

        return Pair.of(plan, plan.getOperatorList());
    }
}
