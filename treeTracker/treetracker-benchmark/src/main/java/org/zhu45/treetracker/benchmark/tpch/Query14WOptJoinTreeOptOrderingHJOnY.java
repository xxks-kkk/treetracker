package org.zhu45.treetracker.benchmark.tpch;

import org.apache.commons.lang3.tuple.Pair;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinNode;
import org.zhu45.treetracker.benchmark.JoinFragmentContext;
import org.zhu45.treetracker.benchmark.Query;
import org.zhu45.treetracker.relational.operator.Operator;
import org.zhu45.treetracker.relational.planner.Plan;

import java.util.Arrays;
import java.util.List;

import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getLineitem;
import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getPart;
import static org.zhu45.treetracker.relational.planner.rule.JoinOrdering.getJoinOrderingFromNodes;

/**
 * Generated by GenerateJOBBenchmarkWithOptimalJoinTrees
 * tpch_int.part(partkey)
 * |tpch.q14w_lineitem(orderkey,partkey,suppkey)
 */
public class Query14WOptJoinTreeOptOrderingHJOnY
        extends Query
{
    public Query14WOptJoinTreeOptOrderingHJOnY(JoinFragmentContext context)
    {
        super(context);
    }

    @Override
    protected Pair<Plan, List<Operator>> constructQuery()
    {
        MultiwayJoinNode partNode = getPart(TPCHQueries.Q14W);
        MultiwayJoinNode lineItemNode = getLineitem(TPCHQueries.Q14W);

        List<MultiwayJoinNode> traversalList = List.of(partNode, lineItemNode);

        Pair<Plan, List<Operator>> pair = createPhysicalPlanFromJoinOrdering(getJoinOrderingFromNodes(traversalList));
        Plan plan = pair.getKey();

        verifyJoinOrdering(plan, Arrays.asList(partNode.getSchemaTableName(),
                lineItemNode.getSchemaTableName()));

        return pair;
    }
}
