package org.zhu45.treetracker.benchmark.tpch;

import org.apache.commons.lang3.tuple.Pair;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinNode;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinOrderedGraph;
import org.zhu45.treetracker.benchmark.JoinFragmentContext;
import org.zhu45.treetracker.benchmark.Query;
import org.zhu45.treetracker.relational.operator.Operator;
import org.zhu45.treetracker.relational.planner.Plan;
import org.zhu45.treetracker.relational.planner.rule.SemiJoinOrdering;

import java.util.Arrays;
import java.util.List;

import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getLineitem;
import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getSupplier;
import static org.zhu45.treetracker.common.DirectedEdge.asDirectedEdge;

/**
 * Generated by GenerateJOBBenchmarkWithOptimalJoinTrees
 *
 * tpch_int.supplier(suppkey,nationkey)
 * |tpch.q15w_lineitem(suppkey)
 */
public class Query15WOptJoinTreeOptOrderingY
        extends Query
{
    public Query15WOptJoinTreeOptOrderingY(JoinFragmentContext context)
    {
        super(context);
    }

    @Override
    protected Pair<Plan, List<Operator>> constructQuery()
    {
        MultiwayJoinNode supplierNode = getSupplier(TPCHQueries.Q15W);
        MultiwayJoinNode lineItemNode = getLineitem(TPCHQueries.Q15W);

        List<MultiwayJoinNode> traversalList = List.of(supplierNode, lineItemNode);

        MultiwayJoinOrderedGraph orderedGraph = new MultiwayJoinOrderedGraph(supplierNode, Arrays.asList(
                asDirectedEdge(supplierNode, lineItemNode)), traversalList);
        SemiJoinOrdering semiJoinOrdering = new SemiJoinOrdering(List.of(Pair.of(supplierNode, lineItemNode)), orderedGraph);

        Plan plan = createPhysicalPlanForYannakakis(semiJoinOrdering);

        verifyJoinOrdering(plan, Arrays.asList(supplierNode.getSchemaTableName(),
                lineItemNode.getSchemaTableName()));

        return Pair.of(plan, plan.getOperatorList());
    }
}
