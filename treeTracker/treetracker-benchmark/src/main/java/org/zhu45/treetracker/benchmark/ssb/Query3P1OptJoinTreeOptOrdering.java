package org.zhu45.treetracker.benchmark.ssb;

import org.apache.commons.lang3.tuple.Pair;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinNode;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinOrderedGraph;
import org.zhu45.treetracker.benchmark.JoinFragmentContext;
import org.zhu45.treetracker.benchmark.Query;
import org.zhu45.treetracker.relational.operator.Operator;
import org.zhu45.treetracker.relational.planner.Plan;

import java.util.Arrays;
import java.util.List;

import static org.zhu45.treetracker.benchmark.ssb.SSBDatabase.getCustomerInt;
import static org.zhu45.treetracker.benchmark.ssb.SSBDatabase.getDateInt;
import static org.zhu45.treetracker.benchmark.ssb.SSBDatabase.getLineOrderInt;
import static org.zhu45.treetracker.benchmark.ssb.SSBDatabase.getSupplierInt;
import static org.zhu45.treetracker.common.DirectedEdge.asDirectedEdge;
import static org.zhu45.treetracker.relational.planner.rule.JoinOrdering.getJoinOrderingFromNodes;

/**
 * Generated by GenerateBenchmarkWithOptimalJoinTrees
 *
 * ssb.q3p1_customer(custkey)
 * |ssb_int.lineorder(lo_orderkey,custkey,partkey,suppkey,datekey)
 * ||ssb.q3p1_supplier(suppkey)
 * ||ssb.q3p1_date(datekey)
 */
public class Query3P1OptJoinTreeOptOrdering
        extends Query
{
    public Query3P1OptJoinTreeOptOrdering(JoinFragmentContext context)
    {
        super(context);
    }

    @Override
    protected Pair<Plan, List<Operator>> constructQuery()
    {
        MultiwayJoinNode lineOrderNode = getLineOrderInt(SSBQueries.Q3P1);
        MultiwayJoinNode dateNode = getDateInt(SSBQueries.Q3P1);
        MultiwayJoinNode customerNode = getCustomerInt(SSBQueries.Q3P1);
        MultiwayJoinNode supplierNode = getSupplierInt(SSBQueries.Q3P1);

        List<MultiwayJoinNode> traversalList = List.of(customerNode, lineOrderNode, supplierNode, dateNode);

        MultiwayJoinOrderedGraph orderedGraph = new MultiwayJoinOrderedGraph(customerNode, Arrays.asList(
                asDirectedEdge(customerNode, lineOrderNode),
                asDirectedEdge(lineOrderNode, supplierNode),
                asDirectedEdge(lineOrderNode, dateNode)), traversalList);

        Plan plan = createPhysicalPlanFromJoinOrdering(
                getJoinOrderingFromNodes(traversalList), orderedGraph);

        verifyJoinOrdering(plan, Arrays.asList(customerNode.getSchemaTableName(),
                lineOrderNode.getSchemaTableName(),
                supplierNode.getSchemaTableName(),
                dateNode.getSchemaTableName()));

        return Pair.of(plan, plan.getOperatorList());
    }
}
