package org.zhu45.treetracker.benchmark.tpch;

import org.apache.commons.lang3.tuple.Pair;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinNode;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinOrderedGraph;
import org.zhu45.treetracker.benchmark.JoinFragmentContext;
import org.zhu45.treetracker.benchmark.Query;
import org.zhu45.treetracker.relational.operator.Operator;
import org.zhu45.treetracker.relational.operator.TupleBasedLeftSemiBloomJoinOperator;
import org.zhu45.treetracker.relational.planner.Plan;
import org.zhu45.treetracker.relational.planner.rule.SemiJoinOrdering;

import java.util.Arrays;
import java.util.List;

import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getNation;
import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getPartsupp;
import static org.zhu45.treetracker.benchmark.tpch.TPCHDatabase.getSupplier;
import static org.zhu45.treetracker.common.DirectedEdge.asDirectedEdge;

/**
 * Generated by GenerateJOBBenchmarkWithOptimalJoinTrees
 * <p>
 * tpch_int.supplier(suppkey,nationkey)
 * |tpch.q11w_nation(nationkey,regionkey)
 * |tpch_int.partsupp(partkey,suppkey)
 */
public class Query11WOptJoinTreeOptOrderingYB
        extends Query
{
    public Query11WOptJoinTreeOptOrderingYB(JoinFragmentContext context)
    {
        super(context);
    }

    @Override
    protected Pair<Plan, List<Operator>> constructQuery()
    {
        MultiwayJoinNode partSuppNode = getPartsupp(TPCHQueries.Q11W);
        MultiwayJoinNode supplierNode = getSupplier(TPCHQueries.Q11W);
        MultiwayJoinNode nationNode = getNation(TPCHQueries.Q11W, null);

        List<MultiwayJoinNode> traversalList = List.of(supplierNode, nationNode, partSuppNode);

        MultiwayJoinOrderedGraph orderedGraph = new MultiwayJoinOrderedGraph(supplierNode, Arrays.asList(
                asDirectedEdge(supplierNode, nationNode),
                asDirectedEdge(supplierNode, partSuppNode)), traversalList);
        SemiJoinOrdering semiJoinOrdering = new SemiJoinOrdering(List.of(Pair.of(supplierNode, nationNode), Pair.of(supplierNode, partSuppNode)), orderedGraph);

        Plan plan = createPhysicalPlanForYannakakis(semiJoinOrdering, TupleBasedLeftSemiBloomJoinOperator.class, false, false, false);

        verifyJoinOrdering(plan, Arrays.asList(supplierNode.getSchemaTableName(),
                nationNode.getSchemaTableName(),
                partSuppNode.getSchemaTableName()));

        return Pair.of(plan, plan.getOperatorList());
    }
}
