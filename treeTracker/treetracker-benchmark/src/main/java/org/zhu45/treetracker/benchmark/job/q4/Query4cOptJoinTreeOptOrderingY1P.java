package org.zhu45.treetracker.benchmark.job.q4;

import org.apache.commons.lang3.tuple.Pair;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinNode;
import org.zhu45.treektracker.multiwayJoin.MultiwayJoinOrderedGraph;
import org.zhu45.treetracker.benchmark.JoinFragmentContext;
import org.zhu45.treetracker.benchmark.Query;
import org.zhu45.treetracker.benchmark.job.JOBQueries;
import org.zhu45.treetracker.relational.operator.Operator;
import org.zhu45.treetracker.relational.operator.TupleBasedLeftSemiHashJoinOperator;
import org.zhu45.treetracker.relational.planner.Plan;
import org.zhu45.treetracker.relational.planner.rule.SemiJoinOrdering;

import java.util.Arrays;
import java.util.List;

import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getInfoTypeInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getKeywordInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getMovieInfoIdxInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getMovieKeywordInt;
import static org.zhu45.treetracker.benchmark.job.IMDBDatabase.getTitleInt;
import static org.zhu45.treetracker.common.DirectedEdge.asDirectedEdge;

/**
 * Generated by GenerateJOBBenchmarkWithOptimalJoinTrees
 *
 * imdb_int.movie_keyword(movie_id,keyword_id)
 * |imdb.q4c_keyword(keyword_id)
 * |imdb.q4c_title(movie_id,kind_id)
 * |imdb.q4c_movie_info_idx(movie_id,info_type_id)
 * ||imdb.q4c_info_type(info_type_id)
 */
public class Query4cOptJoinTreeOptOrderingY1P
        extends Query
{
    public Query4cOptJoinTreeOptOrderingY1P(JoinFragmentContext context)
    {
        super(context);
    }

    @Override
    protected Pair<Plan, List<Operator>> constructQuery()
    {
        MultiwayJoinNode movieInfoIdxNode = getMovieInfoIdxInt(JOBQueries.Q4c, null);
        MultiwayJoinNode movieKeywordNode = getMovieKeywordInt();
        MultiwayJoinNode keywordNode = getKeywordInt(JOBQueries.Q4c);
        MultiwayJoinNode titleNode = getTitleInt(JOBQueries.Q4c, null);
        MultiwayJoinNode infoTypeNode = getInfoTypeInt(JOBQueries.Q4c, null);

        List<MultiwayJoinNode> traversalList = List.of(movieKeywordNode, keywordNode, titleNode, movieInfoIdxNode, infoTypeNode);

        MultiwayJoinOrderedGraph orderedGraph = new MultiwayJoinOrderedGraph(movieKeywordNode, Arrays.asList(
                asDirectedEdge(movieKeywordNode, keywordNode),
                asDirectedEdge(movieKeywordNode, titleNode),
                asDirectedEdge(movieKeywordNode, movieInfoIdxNode),
                asDirectedEdge(movieInfoIdxNode, infoTypeNode)), traversalList);
        SemiJoinOrdering semiJoinOrdering = new SemiJoinOrdering(List.of(Pair.of(movieInfoIdxNode, infoTypeNode), Pair.of(movieKeywordNode, keywordNode), Pair.of(movieKeywordNode, movieInfoIdxNode), Pair.of(movieKeywordNode, titleNode)), orderedGraph);

        Plan plan = createPhysicalPlanForYannakakis(semiJoinOrdering, TupleBasedLeftSemiHashJoinOperator.class, true, false, true);

        verifyJoinOrdering(plan, Arrays.asList(movieKeywordNode.getSchemaTableName(),
                keywordNode.getSchemaTableName(),
                titleNode.getSchemaTableName(),
                movieInfoIdxNode.getSchemaTableName(),
                infoTypeNode.getSchemaTableName()));

        return Pair.of(plan, plan.getOperatorList());
    }
}
